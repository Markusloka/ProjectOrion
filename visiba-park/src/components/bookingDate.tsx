//This file will hold the booking date logic
import React, { useState } from "react";
import ReactDOM from "react-dom";
import date from "./myCalendar";

export const bookDate = () => {
  const bookingDate = { date };
};
